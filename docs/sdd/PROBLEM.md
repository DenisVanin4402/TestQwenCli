я планирую  сделать изменения в о фреймворке  sdd -   docs/sdd,  там есть  файл    SDD_DETAILS.md который описывает многое про фреймворк. Моя задача, трансформировать фреймворк под новые  нужды. минусом этого фрейма -
содатие мастер спецификации по сервису.  для большизх сервисов и слабых моделей  это не одходит. спецификации монструозные. но при этом  часто на проектах есть исчерпывающая документация по  нему  и часто она сохранена в
папке, например  имя_модуля\workflow, имя_модуля\integrations и так далее.  1- хотелось бы иметь возможность подключать  "масстер спецификацию" как существующую папку и мд файлы в ней, а не как один огромный md.  2- нужно
представить такой формат работы, когда аналитик  делает ветку от релизной, делает работу    над мастер спецификацией - меняет  несколько md файлов т.к. знает что нужно делать, и далее коммтитает это в своей ветке.
Разработчик берет  его ветку, и на основе разницы между релизной и   веткой аналитика - получает тот самый change.md и далее идет по процессу, создает  design.md, task.md  и т.д. только без финального  влития в мастер
спецификацию, тк.  там изменения уже есть.Создай детальный план реализацию по этим 2м пунктам - сначала п.1, затем реализацию  п.2 на основе этого плана (планов) далее составим  task.ms и реализуем.  Важно - получение
diff по гиту нужно делать  средствами гита, а также не скачивать локально 2 ветки чтобы срванить их. Задай  уточняющие вопросы  перед тем как начатью
1 -  на первом этапе  требуется подготовиться к реализации изменения фреймворка и составить plan_1_step.md (подключение папки как мастер спецификации) и  plan_2_step.md  (работа  от получения диффа мастер спек в релизной
и  ветке аналитика). на втором этапе создать tasks_1_step.md  и tasks_2_step.md   где будет детальные шаги по изменению фреймворка - при реализации которых мы получим требуемый ф-л.   2-  мастер спецификация  - папка по
пути стандартному для фреймворка - openspec\[service-name]\ -   в нем произвольная структура документов, описывающих систему. нужно предусмотреть инициализацию  - после того как пользователь скопирует документы в эту
папку, чтобы агентам было удобно навигироваться агентам. возможно как раз создать там манифест. но это отдельный шаг в инициализации мастер папки (делается один раз и возможн ообновляется при изменениях. 3  - обратная
совместимость не требуется. 4 -  да, но  нужно предусмотреть что релизная и ветка аналитика  могут задаваться  вручную пользователем. (и не обязательн релизная, просто базовая ветка с мастер папкой и  новая ветка с
измененными \добавленными\удаленными фалами в мастер-папке) т.е. пользователь находится в какой то ветке,  например своей. и хочет получить  change.md  по  ветке1 как базоваой и ветке1.1 - с новыми данными в мастер-папке.
получает дифф и  change.md  и  запускает в своей ветке реализацию.  5 - работа только с локальными ветками. 6- конечно - все файлы в мастер папке могут быть изменены   или добавлены или удалены - в анализ они попадают
безусловно. 7 - это должен быть максимально продуманный документ,  не механистический - т.к. в него нужно включать  контекст остальной документации  мастер папки а не только  механический дифф.  8  -  нужны проверки и
предупреждение, но если пользователь настаивает -  можно проводить
